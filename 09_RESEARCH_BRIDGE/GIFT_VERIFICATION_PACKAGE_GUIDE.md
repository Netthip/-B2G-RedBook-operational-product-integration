# GIFT VERIFICATION PACKAGE — คู่มือชุดตรวจยืนยันการจับคู่ตัวชี้วัด (Gate 4)

**ป้ายกำกับ:** `PRODUCT EVIDENCE — POST-FREEZE` · **ฐานอำนาจ:** `GIFT DECISION` (Issue #1 · `5595281027`) §4 Gate 4
**ผู้บันทึก:** Giho · **สถานะ:** `แยกสองขั้นแล้วใน Pass 2 (B4) — รอ Bo ตรวจ · ยังไม่ใช้กับเอกสารจริง` · ดูหมวด 4 ท้ายเอกสาร

> 🔴 ชุดตรวจนี้ **ไม่แสดงคะแนนของเครื่อง** โดยเจตนา (คำสั่งกิ๊ฟ: *"Do not show detector scores in the
> key-verification view if doing so could bias the key decision"*) — มีเทสต์กันทั้งหน้าเว็บและไฟล์ XLSX
> · ✅ **แยกแล้วใน Pass 2** — หน้าเดิมนี้คือ **POST-RUN ADJUDICATION** และเปิดได้หลังเฉลยถูกผนึกเท่านั้น · ขั้นจัดทำ/ยืนยันเฉลยย้ายไปหน้า `/keybuild` ที่ไม่แสดงผลของ detector (ดูหมวด 4)
> · ทุกแถวรวม 1/2/6/7 แสดงโดยปริยายและผู้ตรวจ override ได้ (เทสต์ `test_indicator_override_1267.py`)

---

## 1. สิ่งที่อยู่ในชุด

**หน้าเว็บ** `/indicators/runs/{run_id}/verify` (ในเครื่องผู้ตรวจ · loopback เท่านั้น)

| คอลัมน์ | มาจาก |
|---|---|
| สถานะที่เครื่องเสนอ + ความหมาย + เหตุผลชนิด (ไม่ใช่คะแนน) | `ind_mappings.proposed_status` · `basis` หลังถอดตัวเลข |
| ข้อความฉบับเก่า / ฉบับใหม่ **ตรงตามที่ปรากฏ** | `ind_observations.observed_text` |
| ขั้นเอกสาร / หน้า ของทั้งสองฝั่ง · ป้าย `ต้องชี้ตำแหน่ง` เมื่อไม่มีกรอบที่เชื่อถือได้ | ทะเบียนเอกสาร + `location_confidence` |
| ความต่างของ นิยาม / หน่วย / วิธีคำนวณ-ตัวหาร / รอบ / ค่าฐาน / ตัวเลข | `differences` |
| ปุ่ม `Old evidence` · `New evidence` · `เทียบข้างกัน + ตัดสิน` | route ที่ควบคุมผ่านทะเบียน (`/evidence/...`) |
| คำตัดสินล่าสุด (ถ้ามี) | `ind_decisions` |

**ไฟล์** `indpkg_<เวลา>_<id>.xlsx` (สร้างจากปุ่มบนหน้า · เก็บที่ `EXPORT_DIR` ของแอป)

* ชีต **ชุดตรวจ** — 27 คอลัมน์: `mapping_id · indicator_id (ชั่วคราว) · สถานะที่เสนอ · ความหมาย · ต้องให้คนยืนยัน · เหตุผลชนิด · ข้อความเก่า · ข้อความใหม่ · ขั้น/ปี/หน้า/ตำแหน่ง/แฮช ของทั้งสองฝั่ง · ความต่าง · ลิงก์เปิดหลักฐานเก่า/ใหม่/เทียบ (route ในระบบ) · ช่องคำตัดสิน · สถานะที่ถูกต้อง · หมายเหตุ · ผู้ตรวจ · เวลา`
* ชีต **วิธีใช้** — ขั้นตอน 5 ข้อ + ตาราง 8 สถานะ
* ชีต **สรุป** — จำนวนคู่ต่อสถานะที่เสนอ · รอคนยืนยัน · ตัดสินแล้ว · บัญชีปิดหรือไม่
* `_snapshot.json` — `run_id · document_id/sha256 ทั้งสองฝั่ง · mapping_version · rules_hash · extractor_version · "scores_included": false`

การป้องกันในไฟล์: กันสูตร (`csv_safe.neutralize`) · `privacy.assert_shareable` · ข้อความจากเอกสาร `INTERNAL_NOT_PUBLISHED`/`UNKNOWN` ถูกแทนด้วยตัวยึด `[ข้อความจากเอกสารที่ยังไม่เผยแพร่ — เปิดดูได้ในระบบเท่านั้น]` · ไม่มี `internal_score`/`trigram_dice`/ลิงก์ไฟล์ในเครื่อง (ตรวจก่อนเขียน ถ้าพบ = ไม่เขียนไฟล์)

---

## 2. วิธีตรวจ (สำหรับกิ๊ฟ)

1. เปิดหน้า `verify` → กดกรองสถานะที่ต้องให้คนยืนยันก่อน (`AMBIGUOUS_REVIEW_REQUIRED` · `DEFINITION_CHANGED` · `SAME_CONCEPT_REPHRASED`)
2. กด **เทียบข้างกัน + ตัดสิน** → เห็นหน้าเก่า/ใหม่พร้อมกรอบเน้น · ข้อความตามที่ปรากฏ · ความต่างนิยาม/หน่วย/วิธี
3. ถ้ากรอบไม่ตรง หรือหน้าไม่มีชั้นข้อความ (`LOCATION_REVIEW_REQUIRED`) → ใช้ปุ่ม **เปิด PDF หน้า n** แล้วชี้ตำแหน่งเอง
4. กรอก **ยอมรับ / ไม่ยอมรับ (ระบุสถานะที่ถูกต้อง) / ยังตัดสินไม่ได้** + เหตุผล + ชื่อผู้ตรวจ → บันทึก (เพิ่มอย่างเดียว · แก้ไม่ได้ · ผูก Audit Trail)
5. `RELATED_NOT_EQUIVALENT` มีให้เลือกในฟอร์มเสมอ แม้เครื่องไม่เคยเสนอ
6. เมื่อครบ กด **สร้างชุดตรวจ XLSX** เป็นบันทึกสรุป (ไฟล์คือสำเนา ไม่ใช่แหล่งจริง — แหล่งจริงคือ `ind_decisions`)

---

## 3. สิ่งที่ยังไม่ทำ / ต้องตัดสิน

| # | เรื่อง | ผู้ตัดสิน |
|---|---|---|
| 1 | ใช้ชุดนี้กับ **เอกสารจริง** ชุดใด (เก่า/ใหม่ · ขั้น · ปี) และวางไฟล์ไว้ที่ใด (นอก repo · นอก OneDrive ที่ sync) | กิ๊ฟ |
| 2 | ชั้นการเปิดเผยของแต่ละเอกสาร (`PUBLIC_PUBLISHED` / `INTERNAL_NOT_PUBLISHED`) — กำหนดว่าข้อความออกในไฟล์ได้หรือไม่ | กิ๊ฟ (ตาม `feedback_disclosure_by_document_stage`) |
| 3 | เกณฑ์ตัวเลขของกติกาจับคู่ (0.75 / 0.50 / 0.05) — ตรึงพร้อม detector ก่อนสร้างเฉลย | Bo → กิ๊ฟ |
| 4 | หน้าที่ไม่มีชั้นข้อความ: กรอกด้วยคน (`manual_observation` · `PAGE_ONLY`) หรือทำ OCR ในรอบถัดไป | กิ๊ฟ |
| 5 | ~~สัญญาอนุญาต backend PDF~~ — ตัดสินแล้ว (Bo `5597645315` ข้อ B): backend ปริยาย = pdfplumber+pypdfium2 · PyMuPDF = extra วิจัย `LICENSE REVIEW REQUIRED` | ปิด |
| 6 | คำตัดสินของกิ๊ฟถือเป็น **เฉลย** ที่ต้องผนึกก่อน Giho รัน detector บนชุดจริง — ต้องกำหนดผู้ถือ hash และเวลาผนึก | กิ๊ฟ |


---

## 4. สองขั้นที่แยกกันแล้ว (B4 · Corrective Pass 2 · commit ระบบ `d04f5d9`)

| ขั้น | หน้า / ตาราง | ผู้ตรวจเห็นอะไร |
|---|---|---|
| **① KEY BUILD / VERIFY** | `/indicators/runs/{run}/keybuild` · `ind_key_entries` | ข้อความสองฝั่งตามที่ปรากฏ · ขั้นเอกสาร/ปี/หน้า/แฮช · ปุ่มเปิดหลักฐานเก่า/ใหม่ · ธง "ข้อความชนขอบหน้า" · 🔴 **ไม่มี** สถานะที่เครื่องเสนอ เหตุผลของเครื่อง หรือคะแนนใด ๆ (เทสต์ตัดบล็อกตัวเลือกในฟอร์มออกแล้วยืนยันว่าไม่มีชื่อสถานะเหลือในหน้า) |
| **② ผนึกเฉลย** | ปุ่มบนหน้าเดียวกัน · `ind_key_seals` | การกระทำของ **custodian** เท่านั้น · บันทึก **แฮชของเฉลย** (`key_digest`) ไม่ใช่เนื้อเฉลย · หลังผนึก **แก้เฉลยไม่ได้** และผนึกซ้ำไม่ได้ |
| **③ POST-RUN ADJUDICATION** | `/indicators/runs/{run}/verify` · `/evidence/compare` · ชุด XLSX · `ind_decisions` | แสดงสถานะที่เครื่องเสนอได้ · 🔴 **ทั้งสามอย่างถูกล็อก (409 `ADJUDICATION_LOCKED_KEY_NOT_SEALED`) จนกว่าเฉลยของรอบนั้นจะถูกผนึก** · ชั้นบริการก็ปฏิเสธการบันทึกคำตัดสินด้วย ไม่ใช่ล็อกแค่หน้าเว็บ |

🔴 **สิ่งที่กลไกนี้บังคับได้ และบังคับไม่ได้**

* บังคับได้: **ลำดับ** — เฉลยต้องเสร็จและถูกผนึกก่อน จึงจะเห็น/ตัดสินสิ่งที่เครื่องเสนอ
* บังคับ**ไม่ได้**: **ตัวบุคคล** — โปรแกรมไม่มีระบบบัญชีผู้ใช้ (SYS-D-20 ยังไม่ implement)
  การแยกสิทธิ์จริงยังต้องมาจากการจัดการนอก repo ตามที่กิ๊ฟกำหนด

🔴 **ยังไม่มีเฉลยจริงของงานวิจัยในระบบ** — การกรอก/ผนึกในชุดทดสอบใช้ **ข้อมูลสังเคราะห์**
เพื่อพิสูจน์ว่ากลไกทำงาน ไม่ใช่การสร้างเฉลย
