# T1B — รายงานการ map layout ชีต `7.x`

**ตอบ:** คำสั่ง Gift (1 กันยายน 2569) · **ผู้ทำ:** Giho
**commit:** `redbook-verify` branch `t1b/fy2570-mvp` → **`0ba32aa`**
**ป้ายกำกับ:** `PRODUCT EVIDENCE — POST-FREEZE`
**สถานะ:** `T1B INSPECTION LAYER IMPLEMENTED — EXTRACTION / VERIFICATION PIPELINE NOT YET COMPLETE`

> 🔒 **`t1b-key-0.1.0` ไม่ถูกเปลี่ยน** — audit rerun ผ่านครบ 8 ข้อด้วยคีย์เดิม
> ไม่พบหลักฐานว่าคีย์ที่ freeze แล้วไม่พอหรือผิด

---

## 1. ปิด 3 review fixes ก่อน (ตามลำดับที่ Gift กำหนด)

| # | ประเด็น | สิ่งที่แก้ |
|---|---|---|
| 1 | **preserve baseline/current side provenance** | เดิม `needs_human` และ `unmapped` ถูกยัดลง `baseline_location` หมด ⇒ finding ของฝั่ง current ชี้ผิดฝั่ง · เพิ่ม `SidedRecord` และ `_one_sided()` |
| 2 | **source locations ของ `PROJECT_ORDINAL_CHANGED`** | เพิ่ม `baseline_location` + `current_location` + `fiscal_year` ครบ |
| 3 | **comparable-year scope** | เดิม union ระดับสมุดงาน ⇒ รายงานว่าปี 2568 เทียบได้ทั้งที่ชีตบทบาท `5.` ของแฟ้ม FY2570 ไม่มีปีนั้น (ปีนั้นมาจากชีตบทบาท `8.`) · เพิ่ม `YearScope` + `scope_of()` คิด **รายขอบเขตชีต** |

**ผลของ fix 3 กับไฟล์จริง** (21011)

| ขอบเขต | เทียบได้ | เฉพาะ baseline | เฉพาะ current |
|---|---|---|---|
| `SECTION_04` | 2568, 2569 | 2567 | 2570 |
| `SECTION_05` | 2569–2572 | 2568 | 2573 |
| `SECTION_08` | 2568–2572 | 2567 | 2573 |

เดิม union ทั้งสมุดงานจะบอกว่าเทียบได้ 2568–2572 ทุกชีต ซึ่งไม่จริงสำหรับบทบาท `5.`

---

## 2. ขอบเขตการ map `7.x` ที่ทำจริง

| ข้อกำหนดของ Gift | สถานะ |
|---|---|
| `7.x` = plan · `7.x.y` = project | ✅ `SECTION_07_PLAN` / `SECTION_07_PROJECT` |
| header row resolve จาก content signature **ห้าม hard-code** | ✅ `find_year_tables()` หาแถวที่มีป้ายปี ≥ 2 ป้าย |
| fiscal year จาก label **ห้ามอิง column letter** | ✅ |
| resolve declared unit จากต้นทาง | ✅ หาคอลัมน์จากคำว่า `หน่วยนับ` — **`7.x` ใช้คอลัมน์ `F` ไม่ใช่ `B`** |
| ผูก project → parent plan | ✅ `parent_plan_norm` จากเลขข้อแม่ แล้วเก็บเป็น**ชื่อแผน** |
| extract amount / indicator / text ที่มีจริง | ✅ |
| raw value + raw title + sheet/cell/row provenance | ✅ ครบทุก record |
| layout ที่ยังไม่ยืนยัน → `UNMAPPED / HUMAN REVIEW` | ✅ `rows_outside_year_table_not_mapped` |

### 2.1 สิ่งที่พบระหว่าง map

| # | ข้อค้นพบ | ผลถ้าไม่แก้ |
|---|---|---|
| 1 | **ชีตหนึ่งมีตารางรายปีหลายชุด** — ชีต `7.x` มีทั้งตารางจำแนกตาม *งบรายจ่าย* และตารางจำแนกตาม *ปีงบประมาณ* | อ่านได้ตารางเดียว ที่เหลือหายเงียบ |
| 2 | **คอลัมน์หน่วยนับไม่คงที่** — บทบาท `5.` ใช้ `B` · `7.x` ใช้ `F` | หน่วยเป็น `UNRESOLVED` ทั้งชีต `7.x` |
| 3 | 🔴 **`Sheet7_1` มี `A1` เป็นหัวบท `7.` แต่แผนจริงอยู่ `A2` (`7.1 แผนงานบุคลากรภาครัฐ`)** | ชีตนี้ได้บทบาท `SECTION_07` (หัวบท) ⇒ **ข้อมูลแผนบุคลากรภาครัฐไม่ถูกจับคู่ข้ามปีเลย** |
| 4 | แถวหัวตารางที่มีป้ายปี **เพียงป้ายเดียว** (บทบาท `8.` มี `B3 = ปี 2567`) | ค่าของปีนั้นหายทั้งคอลัมน์ |
| 5 | การหาข้อความประกาศหน่วยจับคำว่า `หน่วย` ลอย ๆ ไปโดน `"เป้าหมายการให้บริการหน่วยงาน"` | ได้ข้อความที่ไม่ใช่หน่วยนับ |

### 2.2 สิ่งที่ **ยังไม่ map** โดยตั้งใจ

ตารางใน `7.x` ที่จำแนกตาม **งบรายจ่าย** (`งบดำเนินงาน` · `งบลงทุน` · `งบเงินอุดหนุน`
· `งบรายจ่ายอื่น` · `รวม`) ยังออกเป็น `UNMAPPED` พร้อมเหตุผล
`rows_outside_year_table_not_mapped`

**เหตุผล:** ตารางนั้นมีแกนเป็น *ประเภทงบรายจ่าย* ไม่ใช่ *ปีงบประมาณ*
การ map ให้ครบต้องเพิ่มมิติ `budget_category` เข้า **match signature**
— ดูข้อ 6 (`DECISION REQUIRED FROM GIFT`)

---

## 3. mapped / unmapped — ก่อน → หลัง

| ไฟล์ | total | `VALUE` | `UNMAPPED` |
|---|---|---|---|
| FY2569 · 21000 | 441 → **446** | 223 → **223** | 211 → **216** |
| FY2569 · 21011 | 374 → **429** | 251 → **321** | 119 → **100** |
| FY2569 · 21016 | 496 → **590** | 254 → **396** | 237 → **179** |
| FY2570 · 21000 | 443 → **448** | 238 → **238** | 198 → **203** |
| FY2570 · 21011 | 372 → **433** | 251 → **329** | 117 → **96** |
| FY2570 · 21016 | 538 → **650** | 262 → **434** | 272 → **200** |

**แฟ้มระดับกระทรวง (21000) แทบไม่เปลี่ยน** — ถูกต้อง เพราะแฟ้มนั้น**ไม่มีชีต `7.x`**
ที่เพิ่มมา 5 รายการคือแถวนอกตารางรายปีที่เดิมถูกนับรวมอยู่แล้ว

---

## 4. matched / baseline-only / current-only / human

| คู่ | matched | baseline only | current only | human | unmapped | accounting |
|---|---:|---:|---:|---:|---:|---|
| 21011 | 195 → **219** | 60 → 104 | 50 → 97 | 10 → 27 | 236 → 196 | ✅ ตรง |
| 21016 | 202 → **278** | 52 → 122 | 54 → 153 | 15 → 30 | 509 → 379 | ✅ ตรง |
| 21000 | 115 | 105 | 115 | 25 | 409 → 419 | ✅ ตรง |

> `baseline only` / `current only` เพิ่มขึ้นเพราะมีข้อมูลเข้าสู่การจับคู่มากขึ้น
> และเพราะ **fix 3** ทำให้ปีที่ไม่มีในขอบเขตชีตเดียวกันถูกกันออกอย่างถูกต้อง
> (เดิมถูกนับว่าเทียบได้เพราะ union ทั้งสมุดงาน)

---

## 5. collision / ambiguity audit — rerun ผ่านครบ 8 ข้อ

| # | เกณฑ์ | ผล |
|---|---|---|
| 1 | คีย์ชนกันภายในไฟล์ | ✅ **0** |
| 2 | false-split candidates | ✅ **0** |
| 3 | normalize ปี ทำให้ collapse | ✅ **0** |
| 4 | จับ plan เดียวกันข้ามเลขข้อ | ✅ 3 กลุ่ม |
| 5 | `document_level` ชนกัน | ✅ **0** |
| 6 | supporting ชนกับ main | ✅ **0** |
| 7 | ชื่อโครงการซ้ำถูก collapse เงียบ | ✅ **0** |
| 8 | จับคู่โครงการข้ามเลขลำดับ | ✅ 2 คู่ |

**unique keys ต่อไฟล์: 51–61 → 73–118** โดย collision ยังเป็น **0**
⇒ ข้อมูลที่ map เพิ่มเข้ามาไม่ทำให้คีย์ชนกัน

> 🔒 **ไม่พบหลักฐานว่า `t1b-key-0.1.0` ไม่พอหรือผิด** — ไม่เสนอ key version ใหม่

---

## 6. 🔑 `PROJECT_ORDINAL_CHANGED` — ยิงจากไฟล์จริงแล้ว

**หน่วยงาน 21016: 15 finding** (21011 และ 21000 ไม่มี — ถูกต้อง เพราะไม่มีชีตโครงการที่เรียงเลขใหม่)

```
ชื่อโครงการตรงกันแต่เลขลำดับเปลี่ยน
  ฉบับตั้งต้น  'โครงการที่ 2'  (Sheet7.4.2)
  ฉบับปัจจุบัน 'โครงการที่ 3'  (Sheet7.3.3)
  โครงการขับเคลื่อนงานวัคซีนกับองค์กรภาคีเครือข่าย
```

**คนละชื่อชีต · คนละเลขข้อ · คนละเลขลำดับ — แต่เป็นโครงการเดียวกัน**
จับคู่ได้และแจ้งว่าเลขลำดับเปลี่ยน ตรงตามคำตัดสินทาง (ค)

---

## 7. ตัวอย่าง finding ของ plan / project พร้อม source cells

### 7.1 ระดับแผนงาน

```
[PLAN] AMOUNT_DECREASED · ปี 2570
   แผนงาน  : 'แผนงานบุคลากรภาครัฐ'
   รายการ  : 'รวมทั้งสิ้น'
   41.1545 → 39.3760   ผลต่าง −1.7785 ล้านบาท
   baseline Sheet7_1!I7   ·   current Sheet7_1!H7
```

ชีตชื่อเดียวกัน แต่ **คอลัมน์ต่างกัน** (`I` กับ `H`) เพราะปีเลื่อน

### 7.2 ระดับโครงการ

```
[PROJECT] AMOUNT_DECREASED · ปี 2570
   โครงการ : 'โครงการขับเคลื่อนงานวัคซีนกับองค์กรภาคีเครือข่าย'
   ใต้แผน  : 'แผนงานยุทธศาสตร์เสริมสร้างให้คนมีสุขภาวะที่ดี'
   รายการ  : 'รวมทั้งสิ้น'
   1.2076 → 0.7160   ผลต่าง −0.4916 ล้านบาท
   baseline Sheet7.4.2!I24   ·   current Sheet7.3.3!H24
```

🔑 **ชื่อชีตต่างกัน · เลขข้อต่างกัน · เลขลำดับต่างกัน · คอลัมน์ต่างกัน**
แต่จับคู่ได้ถูกต้องด้วย semantic identity — สิ่งที่การเทียบตามตำแหน่งทำไม่ได้เลย

### 7.3 การกระจายของ finding

| ชนิด | 21011 | 21016 |
|---|---:|---:|
| `AMOUNT_UNCHANGED` | 144 | 168 |
| `AMOUNT_DECREASED` | 46 | 26 |
| `AMOUNT_INCREASED` | 28 | 72 |
| `INDICATOR_UNCHANGED` | 0 | 6 |
| `ROW_ADDED` | 97 | 153 |
| `ROW_REMOVED` | 104 | 122 |
| `PROJECT_ORDINAL_CHANGED` | 0 | **15** |
| `NOT_COMPARABLE` | 1 | 6 |
| `YEAR_PRESENT_ON_ONE_SIDE_ONLY` | 16 | 25 |
| `AMBIGUOUS_REQUIRES_HUMAN_REVIEW` | 27 | 30 |
| `UNMAPPED_REQUIRES_HUMAN_REVIEW` | 196 | 379 |

---

## 8. full regression count

> ### ✅ **283 passed · 0 failed**

จาก 274 ก่อนรอบนี้ — เพิ่ม 4 test ของ review fixes และ 5 test ของการ map `7.x`

**test ที่ต้องปรับเพราะพฤติกรรมเปลี่ยนโดยตั้งใจ 2 ข้อ**

| test | เดิมคาดว่า | เหตุผลที่เปลี่ยน |
|---|---|---|
| `test_inv3_role_stable...` | มีบทบาท `SECTION_07` | `Sheet7_1` กลายเป็น `SECTION_07_PLAN` เพราะแผนจริงอยู่ `A2` |
| `test_inspect_aligns_roles...` | มี `SECTION_07_02` / `SECTION_07_03` | บทบาทแผนถูกยุบเป็น `SECTION_07_PLAN` ตาม FM-12 · เปลี่ยนไปเทียบด้วย `section_title` |

---

## 9. `DECISION REQUIRED FROM GIFT`

| # | เรื่อง |
|---|---|
| 1 | **ตารางจำแนกตามงบรายจ่ายใน `7.x`** ยังเป็น `UNMAPPED` · การ map ให้ครบต้องเพิ่มมิติ `budget_category` เข้า **match signature** (`T1BKey` เองไม่ต้องเปลี่ยน เพราะ category เป็น comparison attribute เหมือน `fiscal_year`) — ขอให้ Gift ยืนยันว่านับเป็นการเปลี่ยนคีย์ที่ freeze แล้วหรือไม่ ก่อนที่ Giho จะแตะ |
| 2 | `UNMAPPED` ที่เหลือ 96–216 รายการต่อแฟ้ม มาจากชีตปก · ชีตประกอบ · และตารางงบรายจ่าย — จะ map ต่อในรอบถัดไปหรือปล่อยให้เป็นคิวมนุษย์ |

---

## 10. สิ่งที่ **ไม่ได้แตะ**

`redbook/t1/` · `FlatDataTableAdapter` · `MinistryPdfAdapter` · frozen canonical/schema/rules/version
· raw results · Evidence Index · Chapter 4 · Human Review workbooks · Audit Trail
· `docs/` ของ Bo · ไฟล์ต้นทาง `T1B` · ไฟล์ค้างของสายอื่นใน `reviewpack/`
· **`t1b-key-0.1.0` ที่ freeze แล้ว** · **ยังไม่เริ่ม roll-up / reconciliation**
